apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: gitstratum-object
  namespace: gitstratum-system
  labels:
    app.kubernetes.io/name: gitstratum
    app.kubernetes.io/component: object
    app.kubernetes.io/part-of: gitstratum
spec:
  serviceName: gitstratum-object-headless
  replicas: 3
  podManagementPolicy: Parallel
  selector:
    matchLabels:
      app.kubernetes.io/name: gitstratum
      app.kubernetes.io/component: object
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gitstratum
        app.kubernetes.io/component: object
        app.kubernetes.io/part-of: gitstratum
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9090"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: gitstratum-object
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/name: gitstratum
                  app.kubernetes.io/component: object
              topologyKey: kubernetes.io/hostname
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: gitstratum
                    app.kubernetes.io/component: object
                topologyKey: topology.kubernetes.io/zone
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: gitstratum
              app.kubernetes.io/component: object
      containers:
        - name: object
          image: gitstratum/object:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 9091
              protocol: TCP
            - name: replication
              containerPort: 9093
              protocol: TCP
            - name: metrics
              containerPort: 9090
              protocol: TCP
          env:
            - name: GITSTRATUM_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: GITSTRATUM_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: GITSTRATUM_POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: CONTROL_PLANE_SERVICE
              value: "gitstratum-control-plane-headless.gitstratum-system.svc.cluster.local:9091"
            - name: OBJECT_PEERS
              value: "gitstratum-object-0.gitstratum-object-headless.gitstratum-system.svc.cluster.local:9093,gitstratum-object-1.gitstratum-object-headless.gitstratum-system.svc.cluster.local:9093,gitstratum-object-2.gitstratum-object-headless.gitstratum-system.svc.cluster.local:9093"
            - name: DATA_DIR
              value: "/data"
            - name: ROCKSDB_CACHE_SIZE
              value: "2147483648"
            - name: HOT_OBJECTS_CACHE_SIZE
              value: "2147483648"
            - name: PACK_CACHE_TTL
              value: "5m"
            - name: DELTA_CACHE_ENABLED
              value: "true"
            - name: REPLICATION_FACTOR
              value: "3"
            - name: COMPRESSION_ALGORITHM
              value: "zstd"
            - name: LOG_LEVEL
              value: "info"
            - name: RUST_BACKTRACE
              value: "1"
          resources:
            requests:
              cpu: "2"
              memory: "8Gi"
            limits:
              cpu: "4"
              memory: "16Gi"
          livenessProbe:
            grpc:
              port: 9091
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 9091
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            grpc:
              port: 9091
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 30
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
      terminationGracePeriodSeconds: 120
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          app.kubernetes.io/name: gitstratum
          app.kubernetes.io/component: object
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: fast-ssd
        resources:
          requests:
            storage: 500Gi
